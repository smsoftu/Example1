import { UploadFileInformation } from './upload-file-information';

describe('UploadFileInformation', () => {
  it('should create an instance', () => {
    expect(new UploadFileInformation()).toBeTruthy();
  });
});
